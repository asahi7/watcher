/**
 * A watcher implementation in JS for detecting changes on variables without proxies.
 * If substitution of initial variable is not possible, then this implementation should be used.
 * TODO(aibek): implement this
 **/

const watchArray = (array, handler) => {

}

const watchObject = (object, handler) => {

}
